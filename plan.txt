- 180도로 볼 수 있게 고치기
- 시뮬레이션에서 입력값을 정수형으로
- 시뮬레이션에서 모터 딜레이 시간 구현하기


1. 환경 구성

- 목표 : 특정 두 지점 왔다 갔다가 하기

- 상태 : 라이다 관측값

- 입력 : 
        라이다 관측값(90차원) -> 합성곱연산 -> 16, 18차원으로 줄이기
        목표까지의 x값 차이
        목표까지의 y값 차이
        자동차의 방향 각도

- 행동 : 전진(조향 0), 좌회전(조향 -10), 우회전(조향 +10)

